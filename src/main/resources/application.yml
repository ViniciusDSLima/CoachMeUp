spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 31012004ghf
    username: postgres
    url: jdbc:postgresql://localhost:5432/CoachMeUp
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081

api:
  security:
    token:
     secret :${JWT_SECRET:my-secret-key}
